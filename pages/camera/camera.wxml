  <!-- <image class="add-picture" src="/images/grey/add.png" bindtap="chooseimage"></image>  -->
  <!--view class="photo-container" bindtap="chooseImage">
  <image src="{{tempFilePaths }}" mode="aspecFill"/>
  </view>
   <view class="picinput">
    <input class="input" placeholder="Add a description" placeholder-style="color: #8a8a8a"></input>
  </view>  -->

  <!-- <view class="picinput">
    <input class="input" placeholder="Add location" placeholder-style="color: #8a8a8a"></input></view> -->
  <!-- <view class="post-button">
    <button class="pic-submit-button" bindtap="simpleUpload" plain="true" >Post</button> -->
    <!-- bindtap="chooseimage" -->
  <!-- </view> -->


<view class="page-body">
    <view class="page-section">
        <view class="title-row">
          <image class="camera" src="/images/green/camera.png"/>
          <view class="form-title">Take a Snap!</view>
        </view>
        <view class="page-body-info">
          <block wx:if="{{filePath != ''}}">
            <image src="{{filePath}}" class="image" mode="aspectFit"></image>
          </block>
          <block wx:if="{{filePath === ''}}">
            <view class="image-plus image-plus-nb" bindtap="simpleUpload">
              <view class="image-plus-horizontal"></view>
              <view class="image-plus-vertical"></view>
            </view>
          </block>
        </view>
      <view class="btn-area">
        <view class='lf--cancel' form-type='submit' bindtap="onCancelClicked" >CANCEL</view>
        <view class='lf--submit' form-type='submit'>SUBMIT</view>
      </view>
    </view>
  </view>

  