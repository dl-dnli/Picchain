<!--pages/vindex/vindex.wxml-->
<view class="card-container">
  <view wx:for="{{locations}}" wx:for-item="location" wx:key="location.id" bindtap="onLocationClicked"  class="card" data-location="{{location}}">

  <image class="banner" style="background-image: url('{{location.photo}}');" />

      <view class="name">
        {{location.name}}
      </view>

      <view class="vcontent">
        <view class="prize">
         <image class="prize" src='/images/orange/award.svg' /> 
         <view class="prize_text">{{location.prize}}</view>
        </view>
        <view class="count">
          Photos: 0
        </view>
      </view>


  </view>
</view>
